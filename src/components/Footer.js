import styled from "styled-components";

function Footer() {
  return (
    <FooterContainer>
      <h4>Footer</h4>
    </FooterContainer>
  );
}

const FooterContainer = styled.div``;
export default Footer;
